package fr.maygo.city.events;

import org.bukkit.event.Listener;

public class PlayerMove implements Listener {

//	@EventHandler
//	public void onPlayerMove(PlayerMoveEvent event) {
//		if (event.getPlayer().isOp())
//			return;
//		if (City.INSTANCE.getCity().contains(event.getTo())) {
//			for (House house : City.INSTANCE.getHouses()) {
//				if (house.getContent().contains(event.getTo())) {
//					if(house.getOwner() != null) {
//						event.setCancelled(
//								!house.getOwner().toString().equalsIgnoreCase(event.getPlayer().getUniqueId().toString()));
//					}
//				}
//			}
//		}
//	}

}
